
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Frame, Tk, Canvas, Entry, Text, Button, PhotoImage, Toplevel, messagebox
from build.main_window.add_profile.main import Profile
from build.main_window.activity.main import Activity
from build.main_window.health.main import Health
OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"./assets")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


def mainWindow():
    MainWindow()

class MainWindow(Toplevel):
    def __init__(self, *args, **kwargs):
        Toplevel.__init__(self, *args, **kwargs)
        
        self.geometry("1298x655")
        self.configure(bg = "#0BBC8B")


        canvas = Canvas(
            self,
            bg = "#0BBC8B",
            height = 655,
            width = 1298,
            bd = 0,
            highlightthickness = 0,
            relief = "ridge"
        )

        canvas.place(x = 0, y = 0)
        canvas.create_rectangle(
            266.0,
            0.0,
            1298.0,
            655.0,
            fill="#FFFFFF",
            outline="")
        
        self.sidebar_indicator = Frame(self, background="#FFFFFF")

        self.sidebar_indicator.place(x=0, y=133, height=60, width=7)

        self.button_image_1 = PhotoImage(
            file=relative_to_assets("button_1.png"))
        button_1 = Button(
            self,
            image=self.button_image_1,
            borderwidth=0,
            highlightthickness=0,
            command=self.logout,
            relief="flat"
        )
        button_1.place(
            x=17.0,
            y=584.0,
            width=249.0,
            height=71.0
        )

        self.button_image_2 = PhotoImage(
            file=relative_to_assets("button_2.png"))
        self.heal_btn = Button(
            self,
            image=self.button_image_2,
            borderwidth=0,
            highlightthickness=0,
            command=lambda: self.handle_btn_press(self.heal_btn,"heal"),
            relief="flat"
        )
        self.heal_btn.place(
            x=17.0,
            y=287.0,
            width=249.0,
            height=71.0
        )

        self.button_image_3 = PhotoImage(
            file=relative_to_assets("button_3.png"))
        self.act_btn = Button(
            self,
            image=self.button_image_3,
            borderwidth=0,
            highlightthickness=0,
            command=lambda:self.handle_btn_press(self.act_btn,"act"),
            relief="flat"
        )
        self.act_btn.place(
            x=17.0,
            y=215.0,
            width=249.0,
            height=71.0
        )

        self.button_image_4 = PhotoImage(
            file=relative_to_assets("button_4.png"))
        self.pet_btn = Button(
            self,
            image=self.button_image_4,
            borderwidth=0,
            highlightthickness=0,
            command=lambda: self.handle_btn_press(self.pet_btn, "prof"),
            relief="flat"
        )
        self.pet_btn.place(
            x=17.0,
            y=145.0,
            width=249.0,
            height=71.0
        )

        self.image_image_1 = PhotoImage(
            file=relative_to_assets("image_1.png"))
        image_1 = canvas.create_image(
            133.0,
            90.0,
            image=self.image_image_1
        )
        
        
        self.windows = {
            "prof": Profile(self),
            "act": Activity(self),
            "heal": Health(self)
               
         }
        self.handle_btn_press(self.pet_btn, "prof")
        self.sidebar_indicator.place(x=0, y=145)
        
        self.current_window.place(x=266, y=0, width=1032.0, height=655)
        self.current_window.tkraise()
    
     
    def place_sidebar_indicator(self):
        pass   
    
    def logout(self):
        confirm = messagebox.askyesno(
            "Confirm log-out", "Do you really want to log out?"
        )
        if confirm == True:
            user = None
            self.destroy()
            #LoginPota.gui5.loginPage()    
     
    def handle_btn_press(self, caller, name):
        # Place the sidebar on respective button
        self.sidebar_indicator.place(x=0, y=caller.winfo_y())

        # Hide all screens
        for window in self.windows.values():
            window.place_forget()

        # Set ucrrent Window
        self.current_window = self.windows.get(name)

        # Show the screen of the button pressed
        self.windows[name].place(x=266, y=0, width=1032, height=655)
     

    def handle_dashboard_refresh(self):
        # Recreate the dash window
        self.windows["prof"] = Profile(self)



# Create an instance of MainWindow


if __name__ == "__main__":
   main_window = MainWindow()
   main_window.mainloop()
    
